#VARIABLES
"son espacios que se almacenan en nuestro programa"

a = 1
b = 2
c = a + b
# a, b y c son variables
# sus valores pueden ser cambiados pero la variable permanece con el mismo nombre y posicion dentro del codigo" "sirven para almacenar informacion util

# Las variables deben Declararse y Definirse

Nombre = "Danselix95"
# nombre declara la variable, Danselix la define
# Si queremos definir el nombre de una variable larga, es preferible usar snake-case en lugar de camelCase

numero = 4
print(numero)
numero +=  5
print(numero)
# el mas antes del igual aumenta en 1 el valor anterior de la variable

nombre ="daniel"
print(f"hola {nombre}") 
nombre ="33"
print(f"hola {nombre}") 
# tambien se usan las f strings para concatenar un string con un dato anterior, convirtiendo el dato anterior en texto

# Operadores in/not in (ESTUDIAR Y REPASAR)
print("daniel" in nombre)

del nombre 
# con del eliminamos la variable anteriormente declarada

